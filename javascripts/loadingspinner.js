//let topPremier = (hauteurPage - (hauteurTriangle * 2)) / 2;
//let topDeuxieme = hauteurPage-((hauteurPage - (hauteurTriangle * 2)) / 2);
window.addEventListener("load", ()=>{
    document.querySelector(".triangle").style.display="none";
});